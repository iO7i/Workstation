# Source package v0.4

Baseline: exact delivered 0.3.0-alpha.1 implementation-source ZIP. Existing seven modules are retained.
New files implement ownership/health collection, workspace policy and lifecycle, Copilot wire,
expanded ACP, telemetry ingestion, credential versioning, journal archives and cached discovery links.
Schema4 explicitly migrates schema3 and is backed up; no user database was changed in this delivery.

New commands do not appear in MCP. No live side effects, native tests or Windows calls were executed.
Old evidence is historical. Build/CI requires separate certification approval. No executable/Cargo.lock.

See source state, changed-files, adapter matrix, implementation ledger and explicit limitations.

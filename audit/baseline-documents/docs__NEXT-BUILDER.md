# Continue from 0.4.0-alpha.1

The current task was IMPLEMENTATION ONLY. Do not access AI-PC-Hosam, run a build/test, contact a
provider, launch an agent, resolve a secret, modify Docker or run cleanup until the user separately
authorizes certification. This source package is the next baseline; do not merge an older parallel R0.

After that green light, inspect exact source/checksums and local tooling, resolve a real Cargo.lock,
format/typecheck/lint, execute unit/integration tests and disposable native Windows scenarios.
Do not disable failures or interpret authored test counts as passed. Existing positive ownership
fixture now supplies actual owned-session evidence; a new negative prevents arbitrary reported
bindings from qualifying as retained. Schema tests now expect4 because a real migration was added.

Critical new native scenarios: suspended/owned RPC child teardown; Copilot framing/permission policy;
ACP advertised features/auth; partial process ancestry; same-PID replacement; destination-parent
replacement; Git filters; ignored data; changed workspace before removal; archive interrupted write/
commit and backup dependencies; local secret rotation/revoke/crash fallback; same-user limitations;
read-only MCP canaries; live handoff and quota evidence. Use disposable repositories, not Vertex.

The source contains unsafe Win32 API boundaries and protocol clients that have not been typechecked.
Find and fix native/API incompatibilities before operational use. Never treat a successful compile
as certification of vendor permissions or cleanup safety. No sample secret/provider accounts bundled.

Read IMPLEMENTATION-LEDGER.md and CURRENT-CAPABILITIES.md before reporting progress. The93.27 number
is a coarse bounded source-family tally, NOT final product readiness. Roo/Windsurf native continuation,
Infisical/Bitwarden secret execution, generic automatic activation and licensed benchmark feeds have
not magically become implemented. Preserve those honest unsupported/reference labels.

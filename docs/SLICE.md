> v0.4 update: current semantics and certification hold are in IMPLEMENTATION-V4.md,
> COMMANDS-V4.md and ../CURRENT-CAPABILITIES.md. The following section is preserved baseline context.

# Slice 2 independent implementation

Baseline: workstation-r0-with-discovery-addendum.zip. Not the incompatible separately
released workstation-r0.1-source.zip. Selected baseline and stage commits are in evidence.

User authorization explicitly permits all independent work despite unavailable native
Windows certification. Seven modules are connected through shared SQLite identity,
evidence and records. Source status is PARTIAL, not R0.1 NATIVE GREEN or Slice 2 GREEN.

Implemented domains include bitemporal decisions, scoped resources, work/session/leases,
checkpoint and packet-only continuity, contextual resource discovery, meter-separated
economics, typed diagnostic/plan records, DPAPI source, readonly MCP and project reports.
Real Windows/runtime/provider/agent execution remains a separate blocked gate. The
implementation does not pretend source-only methods are certified integrations.

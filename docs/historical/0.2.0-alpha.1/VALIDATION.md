# Validation — Workstation 0.2.0-alpha.1

**Overall: PARTIAL source candidate. No native Windows release is claimed.**

The latest user instruction permits independent Slice 2 development without waiting
for unavailable native prerequisites. It does not waive actual release certification.

## Executed in this response

| Category | Run | Passed | Failed/errors/skips | What ran |
|---|---:|---:|---:|---|
| Shared database | 56 | 56 | 0 | Exact shipped SQL migrations/query via Python SQLite |
| Git recipes | 12 | 12 | 0 | Actual Git in disposable Linux repositories |
| Economics reference | 28 | 28 | 0 | Independent Python oracle + shared synthetic vectors |
| JSON/catalog/static contracts | 20 | 20 | 0 | Strict schemas, catalog, command/field boundaries |
| **New total** | **116** | **116** | **0** | Not Rust or Windows execution |

Raw IDs, environment and results: `evidence/control-tests.json` and `.log`.
Host: Linux, Python 3.13.5, SQLite 3.46.1,
git version 2.47.3. This Python SQLite is NOT the application's linked version.

## Actually observed behavior

Transactions preserve the original R0 projects/roots/scans on schema upgrade; failure
rolls back. SQL rejects cross-project associations, two primaries, invalid state jumps,
incompatible decision chains and missing environment scope. Expiring a lease does not
release the primary. The exact decision query preserves effective and known timelines.

Actual Git recipes exposed dirty/untracked/ignored/local-ref state while fixture
contents and index hashes stayed unchanged. External filter-name checks did not run
their commands. A same-size edit with restored mtime deliberately demonstrates the
metadata-only freshness blind spot; the packet requires fresh content/test inspection.

Economics tests cover sparse/zero/stale/reset/correction/gapped inputs, multiple
horizons, separate meters, model availability/licensing and explicit scenario assumptions.
An ETA after the current quota reset is suppressed. The source makes no exact combined
subscription-runway claim or automatic provider/model/subscription change.

The synthetic demo executes shipped SQL and the Python oracle; its files explicitly
state that no Rust application, provider or live agent was executed. Its tiny SQL timing
is not native context/MCP/doctor performance certification.

## Not executed

- Rust parsing/type-checking, formatting, Clippy, unit/integration tests or compilation.
- Real Cargo dependency resolution, Cargo.lock or standards-compliant resolved SBOM.
- MSVC/Windows SDK, Job Objects, ACL/reparse/DPAPI native behavior or PowerShell smoke.
- Live provider calls, quota polling, account authentication, credential injection.
- Native vendor resume/launch/ACP or a real Codex-to-Grok handoff.
- Real Docker/Codex repairs, cleanup, process termination or production operations.
- Clean-machine packaging/code signing and full performance gates.

**141 Rust test annotations are authored in the tree; zero were executed here.**
Some are platform-specific and test counts will differ by build target. Record the
actual native count later instead of using this authored count as passed evidence.

## Historical gate reconciliation

The unchanged R0+D1 baseline validator passed 20/20 when rerun against the original
archive. On this extended source it produces 19 passes plus its old prohibition on
`CryptUnprotectData`: the original R0 did not allow any vault implementation, while
Slice 2 explicitly requests DPAPI. The historical test is preserved, not disabled or
claimed green. Current vault privacy/runtime gates are separate and native unrun.
See `evidence/historical-scope-check.json` and `docs/SCOPE-PRECEDENCE.md`.

## Delivery safety

Only generated/container-side source and disposable fixtures were changed. Existing
Windows D:\Ops, Docker tasks/data, active conversations, credentials and user repositories
were not accessed or modified. No credential values or live account data are packaged.
Source ZIP excludes .git, build/cache output, temporary homes, databases and vault blobs.
